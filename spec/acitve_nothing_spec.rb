require 'spec_helper'

describe AcitveNothing do
  it 'has a version number' do
    expect(AcitveNothing::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
